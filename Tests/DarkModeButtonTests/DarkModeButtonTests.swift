    import XCTest
    @testable import DarkModeButton

    final class DarkModeButtonTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(DarkModeButton().text, "Hello, World!")
        }
    }
